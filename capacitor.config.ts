import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ai.actyv.enterprise',
  appName: 'actyv for Enterprise',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
